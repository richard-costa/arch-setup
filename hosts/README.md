# Hosts

Configuration tied to a particular machine.

Use this for things such as:

- monitor layout / connector names
- hardware-specific audio fixes
- laptop lid or power quirks
- device-specific notes

Generic Niri/Fish/Kitty configuration belongs under `dotfiles/`.
